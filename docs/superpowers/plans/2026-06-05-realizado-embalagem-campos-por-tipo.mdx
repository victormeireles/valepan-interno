# Realizado Embalagem — Campos por Tipo — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** No modal de realizado/embalagem, exibir apenas Caixas+Pacotes, Unidades ou Kg conforme a meta original do pedido — não o saldo restante.

**Architecture:** Função pura `resolverCamposRealizadoEmbalagem` no domínio decide visibilidade. A page passa `pedidoMetaOriginal` separado de `pedidoQuantidades` (saldo). O `ProducaoModal` renderiza inputs condicionalmente, zera campos ocultos no submit e valida só dimensões visíveis.

**Tech Stack:** Next.js App Router, React 19, TypeScript, Vitest.

**Spec:** `docs/superpowers/specs/2026-06-05-realizado-embalagem-campos-por-tipo-design.mdx`

---

## Mapa de arquivos

| Arquivo | Responsabilidade |
|---------|------------------|
| `src/domain/embalagem/painel-quantidade.ts` | Tipo + `resolverCamposRealizadoEmbalagem` |
| `src/domain/embalagem/painel-quantidade.test.ts` | Testes unitários da regra |
| `src/domain/realizado/painel-pedido-adapter.ts` | Meta original em `PainelLoteItem` / `loteToPainelItem` |
| `src/app/realizado/embalagem/page.tsx` | Passar `pedidoMetaOriginal` ao modal |
| `src/components/ProducaoModal.tsx` | Render condicional, validação e submit |

---

### Task 1: Função de domínio `resolverCamposRealizadoEmbalagem`

**Files:**
- Modify: `src/domain/embalagem/painel-quantidade.ts`
- Create: `src/domain/embalagem/painel-quantidade.test.ts`

- [ ] **Step 1: Escrever testes que falham**

Criar `src/domain/embalagem/painel-quantidade.test.ts`:

```typescript
import { describe, expect, it } from 'vitest';
import { resolverCamposRealizadoEmbalagem } from '@/domain/embalagem/painel-quantidade';

describe('resolverCamposRealizadoEmbalagem', () => {
  it('meta com caixas libera caixas e pacotes', () => {
    expect(
      resolverCamposRealizadoEmbalagem({ caixas: 225, pacotes: 0, unidades: 0, kg: 0 }),
    ).toEqual({ caixas: true, pacotes: true, unidades: false, kg: false });
  });

  it('meta só com pacotes libera caixas e pacotes', () => {
    expect(
      resolverCamposRealizadoEmbalagem({ caixas: 0, pacotes: 30, unidades: 0, kg: 0 }),
    ).toEqual({ caixas: true, pacotes: true, unidades: false, kg: false });
  });

  it('meta cx + pct libera caixas e pacotes', () => {
    expect(
      resolverCamposRealizadoEmbalagem({ caixas: 10, pacotes: 5, unidades: 0, kg: 0 }),
    ).toEqual({ caixas: true, pacotes: true, unidades: false, kg: false });
  });

  it('meta com unidades libera só unidades', () => {
    expect(
      resolverCamposRealizadoEmbalagem({ caixas: 0, pacotes: 0, unidades: 500, kg: 0 }),
    ).toEqual({ caixas: false, pacotes: false, unidades: true, kg: false });
  });

  it('meta com kg libera só kg', () => {
    expect(
      resolverCamposRealizadoEmbalagem({ caixas: 0, pacotes: 0, unidades: 0, kg: 12 }),
    ).toEqual({ caixas: false, pacotes: false, unidades: false, kg: true });
  });

  it('meta zerada usa fallback (todos visíveis)', () => {
    expect(
      resolverCamposRealizadoEmbalagem({ caixas: 0, pacotes: 0, unidades: 0, kg: 0 }),
    ).toEqual({ caixas: true, pacotes: true, unidades: true, kg: true });
  });
});
```

- [ ] **Step 2: Rodar testes e confirmar falha**

Run: `npm test -- src/domain/embalagem/painel-quantidade.test.ts`

Expected: FAIL — `resolverCamposRealizadoEmbalagem` is not exported

- [ ] **Step 3: Implementar função**

Adicionar ao final de `src/domain/embalagem/painel-quantidade.ts`:

```typescript
export type CamposRealizadoEmbalagem = {
  caixas: boolean;
  pacotes: boolean;
  unidades: boolean;
  kg: boolean;
};

const CAMPOS_TODOS: CamposRealizadoEmbalagem = {
  caixas: true,
  pacotes: true,
  unidades: true,
  kg: true,
};

export function resolverCamposRealizadoEmbalagem(meta: Quantidade): CamposRealizadoEmbalagem {
  if (meta.caixas > 0 || meta.pacotes > 0) {
    return { caixas: true, pacotes: true, unidades: false, kg: false };
  }
  if (meta.unidades > 0) {
    return { caixas: false, pacotes: false, unidades: true, kg: false };
  }
  if (meta.kg > 0) {
    return { caixas: false, pacotes: false, unidades: false, kg: true };
  }
  return CAMPOS_TODOS;
}
```

- [ ] **Step 4: Rodar testes e confirmar sucesso**

Run: `npm test -- src/domain/embalagem/painel-quantidade.test.ts`

Expected: PASS (6 tests)

- [ ] **Step 5: Commit**

```bash
git add src/domain/embalagem/painel-quantidade.ts src/domain/embalagem/painel-quantidade.test.ts
git commit -m "feat(embalagem): resolver campos visíveis do realizado por meta original"
```

---

### Task 2: Meta original no adapter de lote

**Files:**
- Modify: `src/domain/realizado/painel-pedido-adapter.ts`

- [ ] **Step 1: Estender `PainelLoteItem`**

Adicionar campos opcionais ao type:

```typescript
export type PainelLoteItem = RealizadoItemEmbalagem & {
  pedidoEmbalagemId?: string;
  // ... campos existentes ...
  metaCaixas?: number;
  metaPacotes?: number;
  metaUnidades?: number;
  metaKg?: number;
};
```

- [ ] **Step 2: Preencher meta em `loteToPainelItem`**

No return de `loteToPainelItem`, adicionar:

```typescript
pedidoEmbalagemId: pedido.pedidoEmbalagemId,
metaCaixas: pedido.pedido.caixas,
metaPacotes: pedido.pedido.pacotes,
metaUnidades: pedido.pedido.unidades,
metaKg: pedido.pedido.kg,
```

Substituir o bloco que hoje zera `pedidoCaixas` etc. — manter `pedidoCaixas: 0` (saldo não usado no card compacto; edit carrega da API).

- [ ] **Step 3: Commit**

```bash
git add src/domain/realizado/painel-pedido-adapter.ts
git commit -m "feat(embalagem): incluir meta original do pedido no PainelLoteItem"
```

---

### Task 3: Page passa `pedidoMetaOriginal` ao modal

**Files:**
- Modify: `src/app/realizado/embalagem/page.tsx`

- [ ] **Step 1: Novo lote — incluir meta no `editingItem`**

Em `handleNovoLote`, após os campos de saldo (`pedidoCaixas` etc.), adicionar:

```typescript
metaCaixas: pedido.pedido.caixas,
metaPacotes: pedido.pedido.pacotes,
metaUnidades: pedido.pedido.unidades,
metaKg: pedido.pedido.kg,
```

- [ ] **Step 2: Editar lote — preservar meta do item**

Em `handleEditProducao`, o spread `...item` já traz `metaCaixas` etc. de `loteToPainelItem`. Não sobrescrever com zeros.

- [ ] **Step 3: Passar prop ao `ProducaoModal`**

Adicionar prop (junto de `pedidoQuantidades`):

```typescript
pedidoMetaOriginal={
  editingItem
    ? {
        caixas: editingItem.metaCaixas ?? 0,
        pacotes: editingItem.metaPacotes ?? 0,
        unidades: editingItem.metaUnidades ?? 0,
        kg: editingItem.metaKg ?? 0,
      }
    : undefined
}
```

- [ ] **Step 4: Commit**

```bash
git add src/app/realizado/embalagem/page.tsx
git commit -m "feat(embalagem): passar meta original do pedido ao ProducaoModal"
```

---

### Task 4: `ProducaoModal` — render, validação e submit

**Files:**
- Modify: `src/components/ProducaoModal.tsx`

- [ ] **Step 1: Nova prop e helpers**

Importar:

```typescript
import { useMemo } from 'react';
import {
  resolverCamposRealizadoEmbalagem,
  type CamposRealizadoEmbalagem,
} from '@/domain/embalagem/painel-quantidade';
```

Adicionar à interface:

```typescript
pedidoMetaOriginal?: {
  caixas: number;
  pacotes: number;
  unidades: number;
  kg: number;
};
```

Desestruturar `pedidoMetaOriginal` nos props.

Dentro do componente, após os states:

```typescript
const camposVisiveis: CamposRealizadoEmbalagem = useMemo(() => {
  if (mode !== 'embalagem' || !pedidoMetaOriginal) {
    return { caixas: true, pacotes: true, unidades: true, kg: true };
  }
  return resolverCamposRealizadoEmbalagem(pedidoMetaOriginal);
}, [mode, pedidoMetaOriginal]);

const sanitizeQuantidades = useCallback(
  (data: ProducaoData): ProducaoData => {
    if (mode !== 'embalagem') return data;
    return {
      ...data,
      caixas: camposVisiveis.caixas ? data.caixas : 0,
      pacotes: camposVisiveis.pacotes ? data.pacotes : 0,
      unidades: camposVisiveis.unidades ? data.unidades : 0,
      kg: camposVisiveis.kg ? data.kg : 0,
    };
  },
  [mode, camposVisiveis],
);

const totalQtyVisivel = useCallback(
  (data: ProducaoData) => {
    const q = sanitizeQuantidades(data);
    return (q.caixas || 0) + (q.pacotes || 0) + (q.unidades || 0) + (q.kg || 0);
  },
  [sanitizeQuantidades],
);
```

- [ ] **Step 2: Atualizar `isPartialProduction`**

Substituir corpo (modo embalagem):

```typescript
const isPartialProduction = (): boolean => {
  if (mode !== 'embalagem' || !pedidoQuantidades) return false;

  return (
    (camposVisiveis.caixas &&
      pedidoQuantidades.caixas > 0 &&
      formData.caixas < pedidoQuantidades.caixas) ||
    (camposVisiveis.pacotes &&
      pedidoQuantidades.pacotes > 0 &&
      formData.pacotes < pedidoQuantidades.pacotes) ||
    (camposVisiveis.unidades &&
      pedidoQuantidades.unidades > 0 &&
      formData.unidades < pedidoQuantidades.unidades) ||
    (camposVisiveis.kg &&
      pedidoQuantidades.kg > 0 &&
      formData.kg < pedidoQuantidades.kg)
  );
};
```

- [ ] **Step 3: Validar e enviar só campos visíveis**

Em `executeSaveNovoLote`, `executeSavePartial` e qualquer outro ponto que calcula `totalQty` para embalagem:

```typescript
const payload = sanitizeQuantidades(formData);
if (totalQtyVisivel(formData) <= 0) { /* mensagem de erro existente */ }
// usar payload no JSON.stringify / buildLotePayload(payload)
```

Atualizar `buildLotePayload` calls para receber dados sanitizados, ou chamar `buildLotePayload(sanitizeQuantidades(formData))`.

Em `executeSubmit` (fluxo edição via `onSave`), passar dados sanitizados ao `onSave`.

- [ ] **Step 4: Render condicional dos inputs**

Substituir o grid de quantidades (modo embalagem) por:

```tsx
<div className="grid grid-cols-2 gap-6">
  {(mode !== 'embalagem' || camposVisiveis.caixas) && (
    <NumberInput
      label={mode === 'forno' || mode === 'fermentacao' || mode === 'resfriamento' ? 'Latas' : 'Caixas'}
      value={formData.caixas}
      onChange={(value) => setFormData((prev) => ({ ...prev, caixas: value }))}
      min={0}
      step={1}
    />
  )}

  {mode !== 'forno' &&
    mode !== 'fermentacao' &&
    mode !== 'resfriamento' &&
    (mode !== 'embalagem' || camposVisiveis.pacotes) && (
      <NumberInput
        label="Pacotes"
        value={formData.pacotes}
        onChange={(value) => setFormData((prev) => ({ ...prev, pacotes: value }))}
        min={0}
        step={1}
      />
    )}

  {(mode !== 'embalagem' || camposVisiveis.unidades) && (
    <NumberInput
      label="Unidades"
      value={formData.unidades}
      onChange={(value) => setFormData((prev) => ({ ...prev, unidades: value }))}
      min={0}
      step={1}
    />
  )}

  {(mode !== 'embalagem' || camposVisiveis.kg) && (
    <NumberInput
      label="Kg"
      value={formData.kg}
      onChange={(value) => setFormData((prev) => ({ ...prev, kg: value }))}
      min={0}
      step={1}
    />
  )}
</div>
```

Modos forno/fermentação/resfriamento: `camposVisiveis` fica all-true quando `mode !== 'embalagem'`, então comportamento anterior preservado.

- [ ] **Step 5: Verificação manual**

1. Abrir `/realizado/embalagem`
2. Pedido de caixas → modal “Novo lote” mostra só Caixas + Pacotes
3. Pedido de unidades → só Unidades
4. Pedido de kg → só Kg
5. Editar lote existente → mesmos campos conforme meta original
6. Salvar com valor em campo visível → sucesso; campos ocultos vão como 0 no payload

- [ ] **Step 6: Rodar testes**

Run: `npm test`

Expected: all tests PASS

- [ ] **Step 7: Commit**

```bash
git add src/components/ProducaoModal.tsx
git commit -m "feat(embalagem): inputs condicionais no modal de realizado por tipo de pedido"
```

---

## Self-review (spec coverage)

| Requisito da spec | Task |
|-------------------|------|
| `resolverCamposRealizadoEmbalagem` | Task 1 |
| Meta original, não saldo | Tasks 2–3 |
| Prop `pedidoMetaOriginal` | Tasks 3–4 |
| Render condicional embalagem | Task 4 Step 4 |
| Campos ocultos = 0 no submit | Task 4 Step 3 |
| Validação qty > 0 só visíveis | Task 4 Step 3 |
| `isPartialProduction` só visíveis | Task 4 Step 2 |
| Badges saldo inalterados | Nenhuma mudança |
| Testes unitários regra | Task 1 |
| Fora de escopo respeitado | Apenas arquivos listados |
